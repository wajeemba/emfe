# Local dev tooling — Addy Osmani's agent-skills (MIT). Kept project-local,
# not part of the shipped product, so it stays out of the repo.
/.claude/skills/
/.claude/agents/
/.claude/commands/
/.claude/references/
/.claude/hooks/
/.claude/AGENT-SKILLS-LICENSE
/.claude/settings.local.json
/.claude/sdd-cache/
/.claude/.simplify-ignore-cache/

# node
node_modules
dist/

# SvelteKit / build output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env — secrets stay local; .env.example is the committed template
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
test-results

# Coverage
coverage

# dependency-cruiser graph output
/graph.svg
